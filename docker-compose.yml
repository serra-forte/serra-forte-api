version: '3.9'

services:
  postgresql:
    image: bitnami/postgresql
    ports:
      - 5432:5432
    environment:
      - POSTGRESQL_USERNAME=kaioADMIN
      - POSTGRESQL_PASSWORD=884a9504-1396-4d8b-ad89-cd771f2bb804
      - POSTGRESQL_DATABASE=tool_ecommerce
      
  redis:
        image: redis:alpine
        container_name: redis
        ports: 
            - 6379:6379
        volumes:
            - redis:/data
volumes:
  redis:
    driver: local

